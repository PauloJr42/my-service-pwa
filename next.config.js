// next.config.js

/** @type {import('next').NextConfig} */
const nextConfig = {
  // Apenas as configurações padrão ou suportadas devem permanecer aqui.
  // 'reactCompiler' foi removida para resolver o aviso/erro de compilação.
  // Otimizações de desempenho padrão do Next.js já estão ativas por padrão.
  // Você pode adicionar outras configurações como 'images' ou 'output' aqui, se necessário.
};

module.exports = nextConfig;